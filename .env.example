# Optional: enables authorized full-track playback through Spotify Web Playback SDK.
# Create a Spotify Developer app and add your exact https://<host>/premium URL as a redirect URI.
VITE_SPOTIFY_CLIENT_ID=
